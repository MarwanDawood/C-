[user]
	name = Marwan Farrah
	email = <email>
[alias]
    push-for-review = push origin master
    unpushed = log --branches --not --remotes --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
    lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative -20
    lgbig = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
    st = status
    co = checkout
    br = branch
    sh = remote show origin
    sh-delta = show --color --pretty=format:%b
    sh-tag = name-rev --tags --name-only
    adog = log --all --decorate --oneline --graph
[merge]
    tool = KDiff3
[format]
    signoff = true
[sendemail]
    smtpserver = smtp.gmail.com
    smtpserverport = 587
    smtpencryption = tls
    smtpuser = mdy.farrah
    smtppass = <App Password>
